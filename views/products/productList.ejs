<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/productList.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <script src="https://kit.fontawesome.com/f7b22d2da3.js" crossorigin="anonymous"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Roboto&display=swap" rel="stylesheet">
    <title>products | Sneakicks</title>
</head>




<body>
    
    <%- include('../partials/header'); %>

    <main>

        <div class="container">

            <p class="directory">> Home > Disponibles</p>

            <% if (session.category == 'admin') { %>
                <div class="add-container">
                    <a class="add" href="/products/create">
                        <p class="add-text">Añadir producto</p>
                        <i class="fa-solid fa-plus"></i>
                    </a>
                </div>
            <% } %>


            <% if (results){ %>
                <% if (results.avaiable){ %>
                    <section class="recomended">
                        <h3>Disponibles</h3>
                        <div class="preview-container">
                            <% for( let product of results.avaiable ) { %> 
                            <div class="preview-container_item">
                                <a href="/products/<%= product.id %>">
                                    <div class="preview_image">
                                        <img src="/img/products/<%= product.main_picture %>" alt="" class="preview">
                                    </div>
                                    <div class="preview_content">
                                        <h3 class="half-margin"><%= product.brand.name %></h3>
                                        <p class="half-margin"><%= product.whole_name %></p>
                                        <p class="half-margin price"><%= product.price_final %></p>
                                    </div>
                                </a>
                            </div>
                            <% } %>
                        </div>
                    </section>
                <% } %>
    
                <% if (results.unavaiable){ %>
                    <section class="recomended">
                        <h3>Sin Stock</h3>
                        <div class="preview-container">
                            <% for( let product of results.unavaiable ) { %> 
                            <div class="preview-container_item">
                                <a href="/products/<%= product.id %>">
                                    <div class="preview_image">
                                        <img src="/img/products/<%= product.main_picture %>" alt="" class="preview">
                                    </div>
                                    <div class="preview_content">
                                        <h3 class="half-margin"><%= product.brand.name %></h3>
                                        <p class="half-margin"><%= product.whole_name %></p>
                                        <p class="half-margin price"><%= product.price_final %></p>
                                    </div>
                                </a>
                            </div>
                            <% } %>
                        </div>
                    </section>
                <% } %>
            <% } %>

            
            <% if (results){ %>
                <% if (results.productsMen){ %>
                    <% if (results.productsMen.length > 0){ %>
                        <section class="recomended">
                            <h3>Disponibles Men</h3>
                            <div class="preview-container">
                                <% for( let product of results.productsMen ) { %> 
                                <div class="preview-container_item">
                                    <a href="/products/<%= product.id %>">
                                        <div class="preview_image">
                                            <img src="/img/products/<%= product.main_picture %>" alt="" class="preview">
                                        </div>
                                        <div class="preview_content">
                                            <h3 class="half-margin"><%= product.brand.name %></h3>
                                            <p class="half-margin"><%= product.whole_name %></p>
                                            <p class="half-margin price"><%= product.price_final %></p>
                                        </div>
                                    </a>
                                </div>
                                <% } %>
                            </div>
                        </section>
                    <% }else{ %>
                        <h3>Sin Stock</h3>
                    <% } %>
                <% } %>
    
                <% if (results.productsWomen){ %>
                    <% if (results.productsWomen.length > 0){ %>
                        <section class="recomended">
                            <h3>Disponibles Women</h3>
                            <div class="preview-container">
                                <% for( let product of results.productsWomen ) { %> 
                                <div class="preview-container_item">
                                    <a href="/products/<%= product.id %>">
                                        <div class="preview_image">
                                            <img src="/img/products/<%= product.main_picture %>" alt="" class="preview">
                                        </div>
                                        <div class="preview_content">
                                            <h3 class="half-margin"><%= product.brand.name %></h3>
                                            <p class="half-margin"><%= product.whole_name %></p>
                                            <p class="half-margin price"><%= product.price_final %></p>
                                        </div>
                                    </a>
                                </div>
                                <% } %>
                            </div>
                        </section>
                    <% }else{ %>
                        <h3>Sin Stock</h3>
                    <% } %>
                <% } %>
    
                <% if (results.productsKids){ %>
                    <% if (results.productsKids.length > 0){ %>
                        <section class="recomended">
                            <h3>Disponibles Kids</h3>
                            <div class="preview-container">
                                <% for( let product of results.productsKids ) { %> 
                                <div class="preview-container_item">
                                    <a href="/products/<%= product.id %>">
                                        <div class="preview_image">
                                            <img src="/img/products/<%= product.main_picture %>" alt="" class="preview">
                                        </div>
                                        <div class="preview_content">
                                            <h3 class="half-margin"><%= product.brand.name %></h3>
                                            <p class="half-margin"><%= product.whole_name %></p>
                                            <p class="half-margin price"><%= product.price_final %></p>
                                        </div>
                                    </a>
                                </div>
                                <% } %>
                            </div>
                        </section>
                    <% }else{ %>
                        <h3>Sin Stock</h3>
                    <% } %>
                <% } %>


                <% if (results.bargains){ %>
                    <% if (results.bargains.length > 0){ %>
                        <section class="recomended">
                            <h3>Disponibles en Oferta</h3>
                            <div class="preview-container">
                                <% for( let product of results.bargains ) { %> 
                                <div class="preview-container_item">
                                    <a href="/products/<%= product.id %>">
                                        <div class="preview_image">
                                            <img src="/img/products/<%= product.main_picture %>" alt="" class="preview">
                                        </div>
                                        <div class="preview_content">
                                            <h3 class="half-margin"><%= product.brand.name %></h3>
                                            <p class="half-margin"><%= product.whole_name %></p>
                                            <p class="half-margin price"><%= product.price_final %></p>
                                        </div>
                                    </a>
                                </div>
                                <% } %>
                            </div>
                        </section>
                    <% }else{ %>
                        <h3>Sin Stock</h3>
                    <% } %>
                <% } %>
            <% } %>
            
            
            
        
        </div>

    </main>

    <%- include('../partials/footer'); %>

</body>
</html>